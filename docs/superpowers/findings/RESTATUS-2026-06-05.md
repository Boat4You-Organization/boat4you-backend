# Boat4You Backend — Findings RE-STATUS (2026-06-05)

Re-verification of the 2026-05-07..11 pre-production review (`SUMMARY.md` / `REGISTER.md`, 181 findings)
against the **current code at HEAD `d1a121d`**. ~4 weeks of fixes happened since the review.
Method: 6 parallel read-only audits (one per phase), each finding re-checked against today's code.

---

## Headline — then vs now

| | Review (2026-05-11) | **Now (2026-06-05)** |
|---|---|---|
| **CRIT open** | 5 | **0 ✅** |
| **HIGH open** | 30 | **~2 functional** (+ ~6 ops/env/doc) |
| Deploy verdict | 🔴 NOT deploy-ready | 🟡 **critical/high axis essentially clear** — pending a few ops verifications + 1 functional HIGH |

**Bottom line:** every go-live blocker the review named is closed in code. What remains is one real
functional HIGH (F1-064), a handful of **ops/env verifications** (not code), and a long tail of
MED/LOW hardening.

---

## CRIT (5) — ALL FIXED ✅

| ID | Finding | Status | Evidence |
|---|---|---|---|
| **F1-019** | Stripe webhook non-idempotent | ✅ FIXED | `StripePaymentService.kt:216` `claimEventIfNew`; `ProcessedStripeEventRepository` `INSERT … ON CONFLICT DO NOTHING`; `StripeEventIdempotencyService` REQUIRES_NEW; `V1_91__processed_stripe_events.sql` |
| **F3-022** | Stripe webhook idempotency (concretization) | ✅ FIXED | same stack as F1-019 + `expireStripeSessionSafely` + defensive null-checks + `oldestUnpaid` filter |
| **F1-068** | `/public/inquiries/{id}/send-test` email-bombing | ✅ FIXED | moved to `AdminInquiryDebugController` (`@Profile("dev")` + `/admin/inquiries/debug` + `@PreAuthorize SYSTEM_ADMIN`) |
| **F2-043** | V9 test-data migrations run in prod (15+ shared-hash admin accts) | ✅ FIXED | test data relocated to `db/testdata/` (out of default scan); only `application-dev.yml` opts it in; prod seeds ONE real admin via `V1_94__seed_admin_user.sql` (password-must-change) |
| **F5-012** | non-crypto `Random` for passwords/verification codes | ✅ FIXED | `Utils.kt:28` `SecureRandom()`, `DEFAULT_PASSWORD_LENGTH = 16`, full base62 charset |

---

## HIGH (30) — 23 FIXED, remainder mostly ops/env

**FIXED (23):** F1-003 (auth rate-limit), F1-004 (password ≥12), F1-005 (JWT iss/aud + role re-fetch),
F1-020 (file magic-byte), F1-024/F1-026 (Stripe `!!` removed), F1-041 (DevController →/admin+admin-role),
F1-056 (cancel reconciliation), F1-057 (set-password rate-limit), F1-067 (inquiry debug →admin),
F1-069 (inquiry POST rate-limit), F1-070 (image dimension cap), F1-036 (DB creds fail-fast),
F2-022 (cleanup date syntax), F3-001 (NauSys/MMK timeouts), F3-002 (`@Retryable` off state-changers),
F3-023 (session expire before overwrite), F4-001 (scheduler pool 8), F4-002 (ShedLock distributed cron),
F4-009 (ImageUtils native `Mat` leak), F5-001 (Spring AccessDenied handler), F5-002 (no `e.message` leak),
F5-013 (`JWT_SECRET_KEY:?required`), F5-014 (mail creds fail-fast), F6-002 (README test-passwords removed).

**Still open — functional (the real remaining one):**
- **F1-064 (HIGH)** — `/public/yachts/{slug}/offers` still fires **synchronous NauSys/MMK sync on the anonymous request thread** (`YachtController.kt:162,219,282`). Partner-quota exhaustion + DoS amplification from a public endpoint. **#1 remaining code item.**

**Still open — OPS / env-verify (not code):**
- **F3-003 / F3-009 (HIGH)** — NauSys `base-url` still defaults to `http://ws.nausys.com` (`application-prod.yml:18`); creds + customer PII go plaintext unless `NAUSYS_URL=https://…` is set on the VM. **Verify the prod env var** (+ NauSys HTTPS support). MMK already HTTPS.
- **F1-022 (HIGH)** — rate-limiter trusts left-most `X-Forwarded-For`; bypassable until the deferred **Faza-7 nginx** `proxy_add_x_forwarded_for`/`$remote_addr` change.
- **F6-003 (HIGH, ops)** — 298 MB `boat4you-ws-perf-update.tar.gz` on the dev disk (NOT in git). Inspect (`tar -tzf … | head`) + secure-delete if it holds PII.

**Still open — bounded / non-runtime:**
- **F3-024 (HIGH, partial)** — webhook is now retry-safe (idempotency) + email out-of-TX, but the partner confirm-call is still inside the webhook `@Transactional` → partner-success-then-rollback leaves a *manual-reconciliation* drift (by design). Not fully atomic.
- **F1-002 (HIGH, partial)** — static OpenAPI specs still `permitAll()` in Security policy, but **closed in prod** via `api-docs/swagger-ui/add-mappings:false`.
- **F2-044 (HIGH)** — `V1_24__drop_columns.sql` has no rationale comment — **doc-only** (migration already applied; 5-min note for Mario), zero runtime risk.
- **F6-001 (HIGH)** — `Dockerfile` runs as root (no `USER`). **Dev-only** — prod deploys via non-root systemd (`User=cusma2/cusma3`) JAR, not Docker. Moot unless the image ships to prod.

---

## Per-phase tally (FIXED-since / still-OPEN / ops-or-info)

| Phase | Topic | FIXED-since | still OPEN | ops/info/partial | Headline |
|---|---|---|---|---|---|
| 1 | Boundary / attack surface | ~32 | ~24 | ~11 | 2 CRIT both fixed; real-open HIGH = F1-064; F1-037/F1-022 ops |
| 2 | Data layer | ~19 | ~24 | ~8 | **CRIT F2-043 fixed**; only open HIGH (F2-044) is doc-only; indexes + equals/hashCode landed |
| 3 | External integrations | ~16 | ~13 | ~11 | **CRIT F3-022 + Stripe trio fixed**; remaining HIGH = NauSys http (ops) + partial atomicity |
| 4 | Scheduled jobs + native | ~5 | ~4 | ~5 | **ALL 3 HIGH fixed** (pool, ShedLock, image leak); rest MED |
| 5 | Cross-cutting (errors/config) | ~16 | 1 (LOW) | ~4 | **ALL CRIT+HIGH fixed**; essentially done |
| 6 | Repo hygiene + deploy | 4 | 7 | 2 | README fixed; open = Docker root (dev-only), tar.gz (local), ktlint/detekt relaxations |

---

## What still genuinely remains (priority order)

1. **F1-064** — make the public-search → partner-sync **async / off the request thread** (only real functional HIGH).
2. **Ops verifications (Mario, ~30 min):** confirm `NAUSYS_URL=https://…` on VM2/VM3 (F3-003/009); inspect+delete the 298 MB tar.gz (F6-003); the X-Forwarded-File nginx batch (F1-022, Faza-7).
3. **MED/LOW hardening backlog (large, non-blocking):** audit-trail injection (F2-001/004), EAGER→lazy auth-path fetches (F2-013/016/031), PII-in-logs/DB scrub (F3-004/010), email outbox+retry (F3-031), Stripe refund/dispute handlers (F3-027), build-quality gates (F6-007 ktlint, F6-009 detekt).

---

**Re-status complete 2026-06-05.** From "🔴 NOT deploy-ready (5 CRIT + 30 HIGH)" → **0 CRIT, ~2 functional/ops HIGH** remaining. The critical security/payment/scheduler hardening the review demanded has been implemented; the project is in a fundamentally different (much healthier) place than the 2026-05-11 snapshot.
